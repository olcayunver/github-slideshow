#!/bin/sh

set -e

echo "Starting optimized Jekyll server..."

# Development environment with optimizations
JEKYLL_ENV=development bundle exec jekyll serve \
  --host 0.0.0.0 \
  --port 4000 \
  --livereload \
  --incremental \
  --profile \
  --trace \
  "$@"
